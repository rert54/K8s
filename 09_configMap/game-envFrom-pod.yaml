# game-envFrom-pod.yaml

apiVersion: v1
kind: Pod
metadata:
  name: game-envfrom-pod 
spec:
  containers:
  - name: nginx-container
    image: nginx:1.14.2
    envFrom:
       - configMapRef:
           name: game-configmap