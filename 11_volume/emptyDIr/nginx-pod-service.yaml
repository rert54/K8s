# nginx-pod-service.yaml

apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport-service
spec:
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007
  type:
    NodePort