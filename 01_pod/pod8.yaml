apiVersion: v1
kind: Pod

metadata:
  name: pod8
  labels:
    app: nginx
    env: dev

spec:
  containers:
    - name: nginx-container
      image: nginx:1.14.2
      ports:
        - containerPort: 80